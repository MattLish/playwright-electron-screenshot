{
  "name": "playwright-electron-screenshot",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "prestart": "npm run build",
    "start": "electron .",
    "lint": "prettier --check \"**/*.{ts,html,json,md}\"",
    "format": "prettier --write \"**/*.{ts,html,json,md}\"",
    "pretest": "npm run build",
    "test": "playwright test",
    "prepare": "husky"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^22.15.21",
    "husky": "^9.1.7",
    "lint-staged": "^16.0.0",
    "playwright": "^1.52.0",
    "prettier": "^3.5.3",
    "sharp": "^0.34.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "electron": "^36.3.1"
  },
  "lint-staged": {
    "**/*.{ts,html,json,md}": "prettier --write"
  }
}
